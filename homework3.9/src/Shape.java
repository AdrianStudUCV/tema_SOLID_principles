// S – Single Responsibility Principle:
// Each class handles only its own data and logic.

interface Shape {
    double calculateArea(); // O – Open/Closed: extend shapes without changing existing logic
}